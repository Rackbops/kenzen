{
  "name": "@kenzen/server",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "The Kenzen server: Hono, SQLite migrations, ingest, decisions (design.md section 3). Empty scaffold until K4-2.",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
