{
  "name": "@kenzen/contract",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "JSON Schemas (schemas/) + fixtures (fixtures/) + the ajv validators for the inventory/report data model (design.md section 4). The two schemas move to Rackbops/Tooling as the source of truth in Tooling#477 and are vendored back byte-identically.",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    },
    "./suppression": {
      "types": "./dist/suppression.d.ts",
      "default": "./dist/suppression.js"
    }
  },
  "files": [
    "dist",
    "schemas",
    "!dist/**/*.test.*"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc",
    "test": "vitest run"
  },
  "dependencies": {
    "ajv": "^8.17.1"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
